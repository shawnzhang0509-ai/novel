# Upstash Redis (Vercel Marketplace → Upstash，或 upstash.com 免费创建)
# 部署到 Vercel 后，在 Project → Settings → Environment Variables 填这两项：
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 若用 Vercel KV / Marketplace 旧变量名，也可：
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
